/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["unsplash.com", "cdn.example.com", "images.avn.com", "images.unsplash.com"]
  },
};

module.exports = nextConfig;
